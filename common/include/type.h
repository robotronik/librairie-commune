#pragma once

enum class Rotation {
    NONE = 0,
    SHORTEST = 1,
    ANTICLOCKWISE = 2,
    CLOCKWISE = 3
};

enum class Direction {
    NONE = 0,
    SHORTEST = 1,
    FORWARD = 2,
    BACKWARD = 3
};